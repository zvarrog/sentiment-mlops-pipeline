"""Тонкий адаптер для обратной совместимости настроек.

Единый источник истины — scripts.config. Этот модуль просто реэкспортирует
параметры для старого кода, который импортирует scripts.settings.
"""

# ВАЖНО: не добавляем собственной логики и чтения окружения здесь, чтобы не
# дублировать конфигурацию. Все значения берём из scripts.config.

# Разрешаем импорт звёздочкой исключительно как мост совместимости.
# Потребителям рекомендуется перейти на `from scripts.config import settings`.
from scripts.config import *  # noqa: F401,F403
